import { Box, styled } from "@mui/material";

const AppPage: React.FC = () => {
  return (
    <Box flex={9} sx={{ backgroundColor: "lightgreen" }}>
      App Page
    </Box>
  );
};

export default AppPage;
